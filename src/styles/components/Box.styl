@import 'variables.styl';

.box-component
	width: 100%
	height: 100%
	background: $box-color
	.heading
		display: flex
		align-items: center
		margin-right: 6px
		z-index: 2
		width: 100%
		height: 40px
		justify-content: space-between
		.title
			margin: 10px
			font-size: 0.75rem
	.content
		height: 100%