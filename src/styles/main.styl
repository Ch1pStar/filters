@import "components/*.styl"


#controlKit .panel
	& > .head
		display: none
	& > .wrap
		overflow-y: scroll !important
	.scrollBar
		display: none !important
	.group-list
		.group
			.head
				cursor: default
				.label
					cursor: default
	.button
		cursor: default