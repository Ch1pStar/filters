// this.particleTextures = [this.emitterZone.children[1].texture];
if (this.cnt) {
	this.stage.removeChild(this.cnt);
}
this.cnt = new PIXI.particles.ParticleContainer(30000, {
	scale: {{#if Scale}}true{{else}}false{{/if}},
	position: true,
	rotation: {{#if Rotate}}true{{else}}false{{/if}},
	uvs: false,
	alpha: {{#if Alpha}}true{{else}}false{{/if}},
});

this.stage.addChild(this.cnt);

var emitter = new Proton.Emitter();
emitter.maxParticles = 30000;