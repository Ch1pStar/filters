
    if (this.filters.indexOf(godrayFilter) === -1) {
      this.filters.push(godrayFilter);
    }
    var filter = this.filters[this.filters.indexOf(godrayFilter)]
      filter.time = {{time}};
      filter.angle = {{angle}};
      filter.gain = {{gain}};
      filter.lacunarity = {{lacunarity}};