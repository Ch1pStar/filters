{{#ifCond enabled true}}
var embrossFilter = new Filters.EmbrossFilter();;
if (this.filters.length < 1) {
  this.filters.push(embrossFilter);
}

this.filters.forEach(function(filter) {
  if (filter === embrossFilter) {
    filter.strength = {{strength}}
  }else {
    that.filters.push(embrossFilter)
  }
});
{{/ifCond}}