

    if (this.filters.indexOf(bulgePinchFilter) === -1) {
      this.filters.push(bulgePinchFilter);
    }    
    var filter = this.filters[this.filters.indexOf(bulgePinchFilter)]
    filter.radius = {{radius}};
    filter.strength = {{strength}};
    filter.center.x = {{centerX}};
    filter.center.y = '{{centerY}}';