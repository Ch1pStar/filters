
    if (this.filters.indexOf(pixelateFilter) === -1) {
      this.filters.push(pixelateFilter);
    }
    var filter = this.filters[this.filters.indexOf(pixelateFilter)]
    filter.size.x= {{sizeX}};
    filter.size.y = {{sizeY}};