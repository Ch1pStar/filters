
    if (this.filters.indexOf(pixelateFilter) === -1) {
      this.filters.push(pixelateFilter);
    }
    this.filters[this.filters.indexOf(pixelateFilter)].size.x= {{sizeX}};
    this.filters[this.filters.indexOf(pixelateFilter)].size.y = {{sizeY}};