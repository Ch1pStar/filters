
    if (this.filters.indexOf(pixelateFilter) === -1) {
      this.filters.push(pixelateFilter);
    }
    this.filters[this.filters.indexOf(pixelateFilter)].sizeX = {{sizeX}};
    this.filters[this.filters.indexOf(pixelateFilter)].sizeY = {{sizeY}};