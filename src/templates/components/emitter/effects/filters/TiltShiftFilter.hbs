    if (this.filters.indexOf(tiltShiftFilter) === -1) {
      this.filters.push(tiltShiftFilter);
    }
    var filter = this.filters[this.filters.indexOf(tiltShiftFilter)]
    filter.blur = {{blur}};
    filter.gradientBlur = {{gradientBlur}};