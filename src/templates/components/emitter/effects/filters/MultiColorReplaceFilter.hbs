
    if (this.filters.indexOf(multiColorReplaceFilter) === -1) {
      this.filters.push(multiColorReplaceFilter);
    }
    var filter = this.filters[this.filters.indexOf(multiColorReplaceFilter)]
      filter.replacements[0] = ['{{original0}}', '{{target0}}']
      filter.replacements[1] = ['{{original1}}', '{{target1}}']
      filter.replacements[2] = ['{{original2}}', '{{target2}}']
      filter.epsilon = {{epsilon}};
      filter.refresh();