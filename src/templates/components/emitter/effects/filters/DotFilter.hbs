
    if (this.filters.indexOf(dotFilter) === -1) {
      this.filters.push(dotFilter);
    } 
    var filter = this.filters[this.filters.indexOf(dotFilter)]
      filter.scale = {{scale}};      
      filter.angle = {{angle}};