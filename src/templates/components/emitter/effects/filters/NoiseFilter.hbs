
    if (this.filters.indexOf(noiseFilter) === -1) {
      this.filters.push(noiseFilter);
    }
    this.filters[this.filters.indexOf(noiseFilter)].noise = {{noise}};
    this.filters[this.filters.indexOf(noiseFilter)].seed = {{seed}};