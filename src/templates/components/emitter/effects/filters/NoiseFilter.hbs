
    if (this.filters.indexOf(noiseFilter) === -1) {
      this.filters.push(noiseFilter);
    }
    var filter = this.filters[this.filters.indexOf(noiseFilter)]
    filter.noise = {{noise}};
    filter.seed = {{seed}};