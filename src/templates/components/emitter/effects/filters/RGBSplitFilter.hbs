
    if (this.filters.indexOf(rgbSplitFilter) === -1) {
      this.filters.push(rgbSplitFilter);
    }
    var filter = this.filters[this.filters.indexOf(rgbSplitFilter)]
    filter.red[0] = {{redX}};
    filter.red[1] = {{redY}};
    filter.blue[0] = {{blueX}};
    filter.blue[1] = {{blueY}};
    filter.green[0] = {{greenX}};
    filter.green[1] = {{greenY}};