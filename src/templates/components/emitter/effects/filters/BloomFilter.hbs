    if (this.filters.indexOf(bloomFilter) === -1) {
      this.filters.push(bloomFilter);
    }    
    this.filters[this.filters.indexOf(bloomFilter)].blur = {{blur}};
    this.filters[this.filters.indexOf(bloomFilter)].blurX = {{blurX}};
    this.filters[this.filters.indexOf(bloomFilter)].blurY = {{blurY}};
  
