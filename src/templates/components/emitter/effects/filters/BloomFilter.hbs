    if (this.filters.indexOf(bloomFilter) === -1) {
      this.filters.push(bloomFilter);
    }    
    var filter = this.filters[this.filters.indexOf(bloomFilter)]
    filter.blur = {{blur}};
    filter.blurX = {{blurX}};
    filter.blurY = {{blurY}};
  
