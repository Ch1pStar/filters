var displacementFilter = new Filters.DisplacementFilter();
if (this.filters.length < 1) {
  this.filters.push(displacementFilter);
}

this.filters.forEach(function(filter) {
  if (filter === displacementFilter) {
    filter.size = {{size}};
  }else {
    that.filters.push(displacementFilter)
  }
});