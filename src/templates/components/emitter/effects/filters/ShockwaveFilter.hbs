

    if (this.filters.indexOf(shockwaveFilter) === -1) {
      this.filters.push(shockwaveFilter);
    }
    var filter = this.filters[this.filters.indexOf(shockwaveFilter)]
    filter.time = {{time}};
    filter.center[0] = {{centerX}};
    filter.center[1] = {{centerY}};