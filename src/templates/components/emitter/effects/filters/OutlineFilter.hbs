    if (this.filters.indexOf(outlineFilter) === -1) {
      this.filters.push(outlineFilter);
    }
    var filter = this.filters[this.filters.indexOf(outlineFilter)]
    filter.thickness = {{thickness}};
    filter.color = '{{color}}';