
    if (this.filters.indexOf(zoomBlurFilter) === -1) {
      this.filters.push(zoomBlurFilter);      
    } 
var filter = this.filters[this.filters.indexOf(zoomBlurFilter)]
    filter.strength = {{strength}};
    filter.center[0] = {{centerX}};
    filter.center[1] = {{centerY}};
    filter.innerRadius = {{innerRadius}};
    
    