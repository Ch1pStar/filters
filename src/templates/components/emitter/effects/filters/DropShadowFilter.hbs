var dropShadowFilter = new Filters.DropShadowFilter();;
if (this.filters.length < 1) {
  this.filters.push(dropShadowFilter);
}

this.filters.forEach(function(filter) {
  if (filter === dropShadowFilter) {
    filter.blur = {{blur}};
    filter.alpha = {{alpha}};
    filter.distance = {{distance}};
    filter.rotation = {{rotation}};
    filter.color = '{{color}}';
  }else {
    that.filters.push(dropShadowFilter)
  }
});