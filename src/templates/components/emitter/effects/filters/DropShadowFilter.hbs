if (this.filters.indexOf(dropShadowFilter) === -1) {
  this.filters.push(dropShadowFilter);
} 
var filter = this.filters[this.filters.indexOf(dropShadowFilter)]
filter.blur = {{blur}};      
filter.alpha = {{alpha}};
filter.distance = {{distance}};
filter.rotation = {{rotation}};
filter.color = '{{color}}';