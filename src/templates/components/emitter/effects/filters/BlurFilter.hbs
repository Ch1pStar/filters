    if (this.filters.indexOf(blurFilter) === -1) {
      this.filters.push(blurFilter);
    } else {      
      this.filters[this.filters.indexOf(blurFilter)].blur = {{blur}};
      this.filters[this.filters.indexOf(blurFilter)].quality = {{quality}};
    }