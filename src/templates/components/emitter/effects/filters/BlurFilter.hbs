    if (this.filters.indexOf(blurFilter) === -1) {
      this.filters.push(blurFilter);
    } 
    var filter = this.filters[this.filters.indexOf(blurFilter)]
    filter.blur = {{blur}};
    filter.quality = {{quality}};