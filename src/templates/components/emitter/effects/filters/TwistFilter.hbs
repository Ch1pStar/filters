
    if (this.filters.indexOf(twistFilter) === -1) {
      this.filters.push(twistFilter);
    }

    var filter = this.filters[this.filters.indexOf(twistFilter)]
    filter.angle = {{angle}};
    filter.radius = {{radius}};
    filter.offset.x = {{x}};
    filter.offset.y = {{y}};