var twistFilter = new Filters.TwistFilter();;
if (this.filters.length < 1) {
  this.filters.push(twistFilter);
}
var that = this;
this.filters.forEach(function(filter) {
  if (filter === twistFilter) {
    filter.angle = {{angle}};
    filter.radius = {{radius}};      
    filter.x = {{x}};      
    filter.y = {{y}};       
  }else {
    that.filters.push(twistFilter)
  }
});