    if (this.filters.indexOf(glowFilter) === -1) {
      this.filters.push(glowFilter);
    } else {      
      this.filters[this.filters.indexOf(glowFilter)].innerStrength = {{innerStrength}};
      this.filters[this.filters.indexOf(glowFilter)].outerStrength = {{outerStrength}};
      this.filters[this.filters.indexOf(glowFilter)].distance = {{distance}};
      this.filters[this.filters.indexOf(glowFilter)].color = '{{color}}';
    }