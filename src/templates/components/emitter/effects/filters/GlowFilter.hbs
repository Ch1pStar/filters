    if (this.filters.indexOf(glowFilter) === -1) {
      this.filters.push(glowFilter);
    }
    var filter = this.filters[this.filters.indexOf(glowFilter)]
      filter.innerStrength = {{innerStrength}};
      filter.outerStrength = {{outerStrength}};
      filter.distance = {{distance}};
      filter.color = '{{color}}';
      filter.quality = {{quality}};