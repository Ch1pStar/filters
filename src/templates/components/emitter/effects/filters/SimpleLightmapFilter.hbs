    if (this.filters.indexOf(simpleLightmapFilter) === -1) {
      this.filters.push(simpleLightmapFilter);
    }
    this.filters[this.filters.indexOf(simpleLightmapFilter)].texture = this.bgSprite.texture;
    this.filters[this.filters.indexOf(simpleLightmapFilter)].color = '{{color}}';
    this.filters[this.filters.indexOf(simpleLightmapFilter)].alpha = {{alpha}};