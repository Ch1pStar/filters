    if (this.filters.indexOf(simpleLightmapFilter) === -1) {
      this.filters.push(simpleLightmapFilter);
    }
    var filter = this.filters[this.filters.indexOf(simpleLightmapFilter)]
    filter.texture = this.bgSprite.texture;
    filter.color = '{{color}}';
    filter.alpha = {{alpha}};