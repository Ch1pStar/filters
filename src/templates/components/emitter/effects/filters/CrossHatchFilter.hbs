{{#ifCond enabled true}}
var convolutionFilter = new Filters.ConvolutionFilter();;
if (this.filters.length < 1) {
  this.filters.push(convolutionFilter);
}
  
  this.filters.push(convolutionFilter)
});
{{/ifCond}}