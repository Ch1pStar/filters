
    if (this.filters.indexOf(colorMatricFilter) === -1) {
      this.filters.push(colorMatricFilter);
    } 
    var filter = this.filters[this.filters.indexOf(colorMatricFilter)]
    filter.reset = {{reset.isChecked}};
    filter.sepia = {{sepia}};
    filter.negative = {{negative}};
    filter.kodachrome = {{kodachrome}};
    filter.lsd = {{lsd}};
    filter.polaroid = {{polaroid}};
    filter.desaturate = {{desaturate}};
    filter.contrast = {{contrast}};
    filter.greyscale = {{greyscale}};
    filter.predator = {{predator}};
    filter.saturate = {{saturate}};