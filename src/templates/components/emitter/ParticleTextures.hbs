this.particleTextures = this.emitterZone.children.reduce(function(acc, itm, index){
	if(index>0){
		acc.push(itm.texture);
		itm.visible = false;
	}
	return acc;
}, []);
// remove particle container visual graphic
this.emitterZone.children.shift();